# =============================================================================
# VocalForge v1.7 - Complete Python Requirements
# =============================================================================
# AI Music Production Application with:
# - Vocal Pitch Correction (Auto-Tune style)
# - ACE-Step Music Generation
# - Vocal2BGM (Vocal to Full Song)
# - Stem Separation (Demucs)
# - Audio Processing & Analysis
# =============================================================================

# -----------------------------------------------------------------------------
# STEP 1: Install PyTorch with CUDA (REQUIRED - run this FIRST)
# -----------------------------------------------------------------------------
# For CUDA 12.1 (RTX 3070, 4070, 4080, 4090):
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# For CUDA 11.8 (older GPUs like RTX 20xx, GTX 10xx):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# For CPU only (no GPU):
# pip install torch torchvision torchaudio

# -----------------------------------------------------------------------------
# STEP 2: Install all other dependencies
# -----------------------------------------------------------------------------
# pip install -r requirements.txt

# =============================================================================
# CORE DEPENDENCIES
# =============================================================================

# FastAPI Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
httpx>=0.24.0
requests>=2.31.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0
scipy>=1.11.0

# Machine Learning / Numeric
numpy>=1.24.0
torch>=2.1.0

# =============================================================================
# OPTIONAL DEPENDENCIES (for advanced features)
# =============================================================================

# Demucs Stem Separation (if you want to use stem separation feature)
# demucs>=4.0.0

# Pitch detection alternatives (more accurate than librosa's default)
# praat-parselmouth>=0.4.3
# pyworld>=0.3.3

# Progress bars and logging
# tqdm>=4.66.0
# colorama>=0.4.6

# Development & Testing
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# ruff>=0.1.0

# =============================================================================
# SYSTEM REQUIREMENTS (install separately)
# =============================================================================
# 1. FFmpeg - Required for audio encoding/decoding
#    Windows: winget install ffmpeg
#    Or download from: https://ffmpeg.org/download.html
#
# 2. CUDA Toolkit (if not using pip PyTorch with CUDA)
#    CUDA 12.1: https://developer.nvidia.com/cuda-12-1-0-download-archive
#    CUDA 11.8: https://developer.nvidia.com/cuda-11-8-0-download-archive
#
# 3. Git LFS (for large file support)
#    winget install Git.Git.LFS

# =============================================================================
# VERIFICATION
# =============================================================================
# After installation, verify everything works:
# python -c "import torch; print(f'PyTorch: {torch.__version__}')"
# python -c "import librosa; print(f'Librosa: {librosa.__version__}')"
# python -c "import fastapi; print(f'FastAPI: {fastapi.__version__}')"
# python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}')"
python-dotenv 
