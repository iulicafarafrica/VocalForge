# VocalForge v1.6 - Python Requirements
# ─────────────────────────────────────────────────────────────
# Install PyTorch with CUDA 12.1 for RTX 3070:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Then install the rest:
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# API backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# HTTP client for ACE-Step API calls
httpx>=0.24.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0

# ML / numeric
numpy>=1.24.0

# PyTorch — install separately with CUDA (see above)
# torch>=2.1.0
